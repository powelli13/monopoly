defmodule Monopoly.Services.EventRecorder do
  @moduledoc """
  Creates or loads events that occur within a given game.
  """
  alias Monopoly.Models.Event

  def create_event(%Event{} = event) do
  end

  def load_events(game_id) do
  end
end
